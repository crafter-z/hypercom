# 终端显示

主显示区用于查看串口收发的内容。它支持**多标签页**、**上下/左右分屏**，并提供两种显示模式：**TRX**（传统行级文本终端）和 **TTY**（完整交互式终端）。

## 两种模式

每个端口可在操作面板「串口参数」→「模式」中选择：

| 模式 | 说明 | 适用场景 |
| --- | --- | --- |
| **TRX** | 传统行级文本终端，按「行」收发显示，支持滚动、搜索、过滤、时间戳、编码切换、协议解析 | 大多数串口调试、数据回显 |
| **TTY 终端** | 基于 xterm.js 的完整交互终端，支持 ANSI/VT100、光标定位、备用屏幕 | 与设备 shell 交互（如进入 vim / top）、需要完整终端控制序列 |

> **TTY 终端模式**：
> - 数据是字节流，**没有「行」语义**，因此不支持 TRX 的行过滤、协议解析、多编码切换；按 UTF-8 解码。
> - 发送命令时**无本地回显**（由对端设备 / shell 自行回显，避免重复）。
> - 会话跨标签切换保留（切走再切回来不丢失内容），但关闭标签、切换模式、跨分屏拖拽会销毁实例。
> - TTY 端口暂不支持弹出到独立窗口。

## 多标签页

- **打开**：双击左侧串口，或右键 →「在标签页中打开」。
- **切换**：点击标签头；活动标签高亮。
- **关闭**：点击标签上的关闭按钮，或右键菜单。
- **标签右键菜单**：
  - 固定 / 取消固定
  - 关闭、关闭左侧、关闭右侧、关闭其他
  - 打开所有标签页、断开所有标签页
  - 移至分屏
- **拖拽**：标签可拖拽到分屏区域，或拖到另一个分屏的空白处移动。

> 关闭标签页**不会断开串口**——连接和日志保持，重新打开标签页后从零开始新一轮显示。

## 分屏

- **左右分屏**：主显示区工具栏「左右分屏」。
- **上下分屏**：工具栏「上下分屏」。
- **调整**：拖动分屏之间的分隔条。
- **关闭分屏**：分屏空窗格上的「关闭分屏」按钮。
- **跨分屏拖拽**：把标签页拖到另一个分屏中。

## 终端工具栏（TRX 标签页顶部）

| 控件 | 作用 |
| --- | --- |
| 编码选择 | 切换解码编码（UTF-8 / GBK / ASCII / ISO-8859-1…），切换后实时重新解码旧内容 |
| 协议选择 | 绑定协议模板以解析帧（见 [规则与协议](规则与协议.md)） |
| 时间戳 | 是否显示时间戳（每行 / 每轮输出，绝对 / 相对 / 连接时长） |
| 显示格式 | 文本 / HEX 显示接收数据 |
| 滚动锁定 | 固定终端不跟随新数据滚动（点击箭头按钮 / 滚动条两端跳到顶/底） |
| 过滤（全部/仅TX/仅RX） | 只显示 TX 或 RX 方向 |
| 关键词过滤 | 按关键词 / 正则过滤显示内容 |
| 暂停 | 暂停显示（数据继续在后台接收） |
| 回放日志 | 从日志文件回放历史（见 [日志记录](日志记录.md)） |

## 滚动与跟随

- 默认终端**底部锁定**跟随最新数据。
- 滚动到顶部或点击「滚动到顶部」会解锁锁定；点击「滚动到底部」重新锁定。
- **滚动锁定**只由显式操作写入：图钉按钮、顶部/底部跳转按钮、或滚动手势停顿后判定——不会因内容刷新自动解锁。

## 搜索（终端内）

- 按 `Ctrl+F` 聚焦搜索栏。
- 支持**区分大小写**，可「上一个 / 下一个」跳转，命中字符高亮。
- 回车下一个、Shift+回车上一个、Esc 关闭。
- 搜索栏打开时会暂时抑制滚动跟随，关闭后若已锁定则回到最新内容。

## 终端右键菜单

| 菜单项 | 作用 |
| --- | --- |
| 全选 | 全选终端内容 |
| 复制 / 复制为 HEX | 复制选中内容（普通文本或十六进制） |
| 从 HEX 转文本 | 把选中的十六进制转为文本 |
| 导出为 TXT / 导出为 CSV | 导出终端内容 |
| 复制选中行 / 复制可见内容 / 复制全部 | 按行复制 |

## 相关

- [数据收发](数据收发.md)
- [日志记录](日志记录.md)
- [规则与协议](规则与协议.md)
