# 手工测试清单

> 每次发版前逐项验证。使用 SIM:Loopback 虚拟串口完成串口相关测试。

## 零、自动化测试

| 套件 | 命令 | 数量 | 说明 |
|------|------|------|------|
| 前端 (vitest) | `npm run test:run` | 71 tests / 2 files | `useAppStore.test.ts` (49 cases) + `highlightEngine.test.ts` (22 cases) |
| 后端 (cargo) | `cargo test --lib` | 24 tests | logger 编码/分片/反向索引等 |
| TypeScript | `npx tsc --noEmit` | 0 errors | 类型检查 |
| Rust | `cargo check` | 0 errors, 0 warnings | 快速编译检查 |

发版前四项必须全绿。

## 一、串口通信 (7 项)

| # | 步骤 | 预期 |
|---|------|------|
| 1 | 点击 Flask 图标启用模拟模式，侧边栏出现 SIM:Loopback | 模拟串口显示在列表中 |
| 2 | 双击 SIM:Loopback 打开标签页，点击连接按钮 | 状态变为红色"已连接"，终端出现心跳 `[SIM] Heartbeat @ HH:MM:SS` |
| 3 | 在发送框输入 `hello`，点击发送 | 终端出现 TX 行 `>>>>>>SEND>>>>>>>> hello`，接着出现 RX 回显 `Received: hello` |
| 4 | 状态栏右侧 TX/RX 数字增长 | TX、RX 字节数随着收发递增 |
| 5 | 修改波特率为 921600，数据位 7，校验 Even，点击连接 | 连接成功，终端工具栏显示新参数 |
| 6 | 点击断开按钮 | 状态变为绿色"未连接"，心跳停止 |
| 7 | 勾选 HEX 发送，输入 `48 65 6C 6F`，点击发送 | 终端收到回显 `Received: 48 65 6C 6F` |

## 二、终端显示 (6 项)

| # | 步骤 | 预期 |
|---|------|------|
| 8 | 串口持续输出 30+ 行数据 | 终端出现纵向滚动条，内容可上下滚动 |
| 9 | 手动向上滚动 | 输出不再自动跟随（智能滚动暂停） |
| 10 | 滚到底部 | 自动跟随恢复 |
| 11 | 勾选"滚动锁定" | 输出不再滚底 |
| 12 | 切换显示格式为 HEX | 终端内容变为 `48 65 6C 6C 6F` 格式的十六进制显示 |
| 13 | 点击清屏按钮 | 终端内容清空，流量统计不变 |

## 三、配置与规则 (6 项)

| # | 步骤 | 预期 |
|---|------|------|
| 14 | 打开设置 → 语法高亮规则 → 新建规则集，添加规则 `error`，#ff0000，加粗 | 保存后关闭设置 |
| 15 | 向串口发送数据包含 "error" 字样 | 终端中 "error" 显示为红色加粗 |
| 16 | 打开设置 → 发送命令规则 → 新建规则集，添加 3 条命令，勾选循环 | 保存后关闭设置 |
| 17 | 在操作面板选择该命令集，点击"开始循环" | 按顺序循环发送 3 条命令 |
| 18 | 修改配置后点击保存，关闭应用重开 | 配置、规则集、命令集均保留 |
| 19 | 修改配置后点击取消 | 修改不生效 |

## 四、UI 交互 (6 项)

| # | 步骤 | 预期 |
|---|------|------|
| 20 | 在侧边栏拖拽串口 | 串口顺序改变 |
| 21 | 点击左右分屏按钮 | 出现两个分屏区域，可拖拽分割线 |
| 22 | 右键点击标签页 → 移动到分屏 | 标签页迁移到另一分屏 |
| 23 | 右键终端 → 导出为 TXT | 剪贴板获得带时间戳的文本 |
| 24 | Ctrl+滚轮在终端区滚动 | 字体大小改变 |
| 25 | Up 箭头在发送框按下 | 调出历史发送记录 |
