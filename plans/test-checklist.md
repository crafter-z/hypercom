# 手工测试清单

> 每次发版前逐项验证。串口相关测试使用 SIM:Loopback 虚拟串口（真实硬件测试见 `plans/hardware-matrix.md`）。
> 更新日期: 2026-08-15 · 覆盖 Phase A-D 全部新功能 + issue #6/#11/#12 回归

## 零、自动化测试

| 套件 | 命令 | 数量 | 说明 |
|------|------|------|------|
| 前端 (vitest) | `npm run test:run` | 587 tests / 30 files | useAppStore (61) + useRuleStore (48) + useTerminalStore (37) + rxAssembler (32) + rxPipeline (31) + triggerEngine (26) + terminalSearch (25) + sendUtils (23) + highlightEngine (22) + textSend (21) + timeFormat (20) + followLogic (20) + protocolParser (18) + useOperationStore (18) + sendStrip (16) + lineFilter (16) + portSort (12) + logReplay (10) + useToastStore (10) + sendGuard (9) + protocolRenderer (8) + useSerialPorts (8) + protocolE2E (7) + groupTool (7) + diagLog (5) + DisconnectBanner (5) + configMerge (4) + **updateService (19, issue #12: shouldAutoCheck 7 天周期/snooze/首启立即 + runCheck 三分支 + manualCheck 不过 DEV 门控 + localStorage 记账含 clearLastCheck)** + **channel (1, issue #12 复审后仅 channelLabelKey；detectChannel/isPreviewVersion 死代码已删)** |
| 后端 (cargo) | `cargo test --lib` | 136 tests（Windows 运行数） | config (18) + logger (58) + system_cmds (12) + system (10, 平台子集) + diaglog (5) + popout (4) + lib (2) + serial (21, 其中 16 个 `#[cfg(not(target_os = "windows"))]` 非 Windows 运行) + **update.rs (7, issue #12: GitHub API preview tag 解析/严格匹配 + 复审 semver 最大选择（乱序 API/数值比较非词典序）)** |
| E2E (Playwright) | `npx playwright test` | 16 tests | Chromium smoke: app-root / TitleBar / Sidebar / MainDisplay / StatusBar / OperationPanel 渲染 + 状态栏内存预算指示 + 通用设置内存预算双输入（issue #6-2/#6-6）+ RX 显示/隐藏窗口排空（issue #6-10）+ 通知中心串口 chip + 自定义右键菜单（issue #7）+ **更新弹窗冒烟（mock check_for_update）与永不提醒同步设置项（issue #12）** |
| TypeScript | `npx tsc --noEmit` | 0 errors | 类型检查 |
| Rust | `cargo check` | 0 errors, 0 warnings | 快速编译检查 |

发版前四项必须全绿。

## 一、串口通信 (9 项)

| # | 步骤 | 预期 |
|---|------|------|
| 1 | 点击 Flask 图标启用模拟模式，侧边栏出现 SIM:Loopback | 模拟串口显示在列表中 |
| 2 | 双击 SIM:Loopback 打开标签页，点击连接按钮 | 状态变为红色"已连接"，终端出现心跳 `[SIM] Heartbeat @ HH:MM:SS` |
| 3 | 在发送框输入 `hello`，点击发送 | 终端出现 TX 行，接着出现 RX 回显 |
| 4 | 状态栏右侧 TX/RX 数字增长 | TX、RX 字节数随着收发递增 |
| 5 | 修改波特率为 921600，数据位 7，校验 Even，点击连接 | 连接成功，终端工具栏显示新参数 |
| 6 | 点击断开按钮 | 状态变为绿色"未连接"，心跳停止 |
| 7 | 勾选 HEX 发送，输入 `48 65 6C 6F`，点击发送 | 终端收到回显 `Received: 48 65 6C 6F` |
| 8 | 波特率下拉选择"其他..."，输入 1000000，连接 | 自定义波特率生效，连接成功 |
| 9 | 断开模拟模式后重新启用 | SIM:Loopback 重新出现，可再次连接 |

## 二、终端显示 (10 项)

| # | 步骤 | 预期 |
|---|------|------|
| 10 | 串口持续输出 30+ 行数据 | 终端出现纵向滚动条，内容可上下滚动 |
| 11 | 手动向上滚动 | 输出不再自动跟随（智能滚动暂停） |
| 12 | 滚到底部 | 自动跟随恢复 |
| 13 | 勾选"滚动锁定" | 输出不再滚底 |
| 14 | 切换显示格式为 HEX | 终端内容变为十六进制显示 |
| 15 | 点击清屏按钮 | 终端内容清空，**流量统计不变** |
| 16 | Ctrl+F 打开搜索栏，输入关键字 | 搜索栏出现，匹配行高亮，显示 n/m 计数 |
| 17 | 按 Enter / F3 跳下一个匹配 | 终端滚动到匹配行（穿透虚拟滚动视口） |
| 18 | Shift+点击终端行扩展选择范围 | 多行被选中（半透明高亮），右键"复制选中行"可复制全部选中内容 |
| 19 | 右键终端 → "复制全部" | 剪贴板获得全部终端文本（含屏幕外行） |

## 三、配置与规则 (8 项)

| # | 步骤 | 预期 |
|---|------|------|
| 20 | 打开设置 → 语法高亮规则 → 新建规则集，添加规则 `error`，#ff0000，加粗 | 保存后关闭设置 |
| 21 | 向串口发送数据包含 "error" 字样 | 终端中 "error" 显示为红色加粗 |
| 22 | 打开设置 → 发送命令规则 → 新建规则集，添加 3 条命令，勾选循环 | 保存后关闭设置 |
| 23 | 在操作面板选择该命令集，点击"开始循环" | 按顺序循环发送 3 条命令 |
| 24 | 修改配置后点击保存，关闭应用重开 | 配置、规则集、命令集均保留 |
| 25 | 修改配置后点击取消 | 修改不生效（回滚到打开前的状态） |
| 26 | 设置 → 显示 → 时间戳格式切换为"相对时间" | 终端时间戳变为 `+Nms` / `+N.Ns` 格式 |
| 27 | 设置 → 显示 → 时间戳显示方式切换为"每轮一个" | 同一接收块内仅首行显示时间戳 |
| 27a | 设置 → 条件触发 → 新建规则：匹配方式「包含」、pattern `TEST`、动作「弹出提示」、指定串口 SIM:Loopback → 保存后向该端口发送含 `TEST` 的数据 | 弹出 Toast 告警（含端口与规则名）；向**其它**端口发送含 `TEST` 数据不弹（portId 过滤生效） |
| 27b | 设置 → 条件触发 → 新建规则：动作「自动回复」、内容 `ACK\r\n` → 向指定端口发送含匹配 pattern 的数据 | 端口自动收到 `ACK` 回显（终端出现 TX 行） |
| 27c | 设置 → 条件触发 → 动作下拉 | 无「标记书签」选项；旧配置含 bookmark 规则时启动自动归一为「弹出提示」 |
| 27d | 设置 → 日志 → 取消勾选「日志行包含时间戳」与「日志行包含 RX/TX 方向标记」→ 重连端口收发数据 → 打开日志文件 | 日志行无 `[时间戳]` 与 `RX/TX` 前缀，仅纯数据行（再勾选恢复） |
| 27e | 波特率下拉选「其他...」，输入 `96000`（或任意非预设值）| 输入框不消失、可完整键入（不再因命中预设值被卸载）；输入后点连接，自定义波特率生效；清空输入不回弹 |
| 27f | 设置 → 通用 | 界面无「背景图片」设置项 |
| 27g | 串口输出控制栏左侧 | 无漏斗图标，「全部/仅TX/仅RX」切换直接显示 |
| 27h | 连接串口持续高速输出（超 10000 行缓冲）并开启滚动锁定 | 视图持续钉在最新一行，滚动锁定图标保持点亮，位置不脱节 |
| 27i | 侧边栏右键串口 → 设置备注名 → 重启应用 | 备注名随 config.json 持久化，重启后仍显示在串口旁（不再只显示串口号）；隐藏状态同样保留 |
| 27j | 新建分组 → 拖入串口 → 重启应用 | 分组及成员关系保留（分组自动保存不再被全量 config 保存覆盖） |
| 27k | 打开设置 → 修改任意设置并保存 → 重启应用 | 之前保存的分组与备注名仍保留（`set_config` 全量保存不会覆盖 `port_groups`/`port_meta`） |

## 四、UI 交互 (10 项)

| # | 步骤 | 预期 |
|---|------|------|
| 28 | 在侧边栏拖拽串口 | 串口顺序改变 |
| 29 | 点击当前 Pane 的 TabBar 右端分屏按钮 (↑/←) | 出现两个分屏区域，可拖拽分割线（split 按钮已迁至每个 Pane 自己的 TabBar 右端；Toolbar 行已删除） |
| 30 | 在分屏内再次分屏（3 层嵌套） | 树状嵌套正常渲染，各层分割线可拖拽 |
| 31 | 拖拽标签到另一个分屏 | 标签迁移到目标分屏 |
| 32 | 右键终端 → 导出为 TXT | **弹出文件保存对话框**，选择路径后生成 .txt 文件（非剪贴板） |
| 33 | Ctrl+滚轮在终端区滚动 | 字体大小改变 |
| 34 | Up 箭头在发送框按下 | 调出历史发送记录（从进程内 per-port Map 加载；会话期间有效，重启为空） |
| 35 | 发送框输入文本，观察字节计数芯片 | 显示正确的字节数（HEX 模式计解析后字节，文本模式计编码后字节） |
| 36 | 设置 → 通用 → 勾选"回车键插入换行"，发送框按 Enter | 输入框内换行而非发送（默认 Enter 发送，Shift/Ctrl+Enter 永远换行） |
| 37 | HEX 模式下观察输入框下方提示 | 显示"末尾追加: 0D 0A"（或对应行结束符） |

## 五、错误提示 & 连接恢复 (7 项)

| # | 步骤 | 预期 |
|---|------|------|
| 38 | 尝试打开一个不存在的端口（或占用中的端口） | Toast 错误提示出现（红色，6s 自动消失） |
| 39 | 连接 SIM:Loopback 后，在设置中启用 autoReconnect，断开模拟模式 | Toast 警告"SIM:Loopback 已断开"，StatusBar 红色横幅闪烁，Tab 标题出现 ⚠ 图标 |
| 40 | 重新启用模拟模式（端口名相同） | 自动重连成功，Toast 提示"重连成功"，横幅消失，⚠ 消失 |
| 41 | autoReconnect 关闭时断开 | 仅 Toast 警告 + 横幅 + ⚠，不自动重连 |
| 42 | 串口参数区修改波特率等参数（串口已连接） | 参数**立即生效**（无需重连），侧边栏与标题栏波特率同步更新；断开重连后仍保持新参数（不再回退 115200） |
| 43 | 发送失败（如端口已断开时发送） | Toast 错误提示 |
| 44 | 日志写入失败（如磁盘满模拟） | Toast 错误提示 |

## 六、发送区增强 (5 项)

| # | 步骤 | 预期 |
|---|------|------|
| 45 | 设置 → 通用 → 快捷发送槽填入 `AT\r\n` | SendSection 出现快捷按钮，点击即发送 |
| 46 | 发送 3 条不同内容（含 HEX 与字符串），关闭应用重开 | 发送历史为空（session-only：进程内 per-port Map, 上限 50；重启自然丢失，符合用户决策）。会话期间 Up/Down 箭头可回忆 |
| 47 | 关闭并重启应用 | 发送历史为空（session-only；重启自然丢失）。右键"清除历史"按钮已移除 |
| 48 | 快捷发送槽全部为空 | SendSection 不显示快捷按钮区域 |
| 49 | 发送历史超过 50 条 | 最旧的记录被自动删除，仅保留最新 50 条 |

## 七、首启体验 (3 项)

| # | 步骤 | 预期 |
|---|------|------|
| 50 | 首次启动（无串口、无 SIM） | Sidebar 显示引导卡："未检测到串口设备" + 模拟模式按钮 + 快速入门链接 |
| 51 | 点击 TitleBar 关于按钮 | 弹出「关于 HyperCom」弹窗 |
| 52 | 关于弹窗操作 | 版本号显示真实版本（如 `v0.3.3`，不再有 `v0.1.0`）；「GitHub 仓库」按钮可打开仓库链接；「开源许可证」按钮弹出依赖许可证表格；帮助按钮不存在（已被移除） |
| 52a | 关于 → 诊断日志按钮 | 弹出诊断日志对话框，显示后端 `log` 与前端 `console` 统一落盘的内容（含时间戳/级别/来源行） |
| 52b | 诊断日志对话框工具栏 | 级别过滤（全部/INFO/WARN/ERROR）生效；「自动刷新」开启时日志实时增长；手动滚动后不强制跳底 |
| 52c | 诊断日志 → 导出 / 清空 | 导出弹出保存对话框并生成 .log 文件；清空后日志为空（含前端待刷缓冲一并清空） |
| 52d | 诊断日志开关 | 取消勾选「启用诊断日志」并保存后，新日志不再写入文件；重新勾选恢复 |

## 八、日志 & 编码 (6 项)

| # | 步骤 | 预期 |
|---|------|------|
| 54 | 连接端口后检查日志目录 | 自动创建日志文件，内容实时写入 |
| 55 | 日志分片阈值设小（如 1KB），持续接收数据 | 自动分片，旧文件关闭，新文件续写 |
| 56 | 切换终端编码为 GBK，发送中文 | 终端正确显示中文（非乱码） |
| 57 | 切换终端编码为 ISO-8859-1 | 高字节字符正确显示 |
| 57a | 已接收 20 行 UTF-8 数据后，切换编码为 GBK 再切回 UTF-8 | 切 GBK 后存量行立即从 `rawData` 重解码为 GBK；切回 UTF-8 后再立即重解码为 UTF-8（非只影响新数据） |
| 58 | 右键终端 → 导出为 CSV | 弹出保存对话框，CSV 文件含时间戳/方向/内容三列 |

## 九、协议解析 (3 项)

| # | 步骤 | 预期 |
|---|------|------|
| 59 | 设置 → 协议模板 → 新建模板（帧头 AA 55，长度 1 字节，校验 sum8） | 保存成功 |
| 60 | 终端工具栏选择该协议模板 | 后续接收数据按协议帧解析，字段着色显示 |
| 61 | 发送一个校验错误的帧 | 校验字段红色高亮显示 |

## 十、安装 & 构建 (3 项)

| # | 步骤 | 预期 |
|---|------|------|
| 62 | `npm run tauri build` | 成功产出 NSIS 安装器 + MSI |
| 63 | 运行 NSIS 安装器 | 双语选择（简中/English），安装到 Program Files，创建开始菜单快捷方式 |
| 64 | 卸载 HyperCom | 程序文件清理干净 |

## 十一、issue #6 回归 (6 项)

| # | 步骤 | 预期 |
|---|------|------|
| 65 | 设置 → 通用 → 内存：修改「内存总预算」与「每端口内存预算」并保存 | 两个输入均可调、保存后重启保留；越界值被 clamp（总预算 [64,8192]MB、每端口 [16,2048]MB） |
| 66 | 连接 SIM:Loopback 并持续灌入大流量（如循环发送长报文）直到超过每端口预算 | 终端自动裁剪最早一半内容，弹出「因内存限制已清除 {{port}} 最早一半的终端内容」toast（每端口 10s 节流，不刷屏）；持续输出不再无限占用内存 |
| 67 | 点击侧边栏工具栏「按端口号排序」 | ports 与各分组组内顺序按 COM1<COM2<COM12 自然序重排；排完仍可拖拽/分组；重启后组内顺序保留、未分组顺序不保留 |
| 68 | 串口右键菜单：未分组且有组时选「移入分组『名称』」/ 无组时选「新建分组并移入」；已在组内选「移出分组」 | 菜单项按端口分组态正确显示；分组关系立即生效并自动保存（重启保留） |
| 69 | 独立发送面板 → 文本模式 → 光标停在某行 → 点「执行当前行并移至下一行」 | 当前行被执行，光标/选区移到下一行行首；在最后一行执行后不再下移 |
| 70 | 设置弹窗内框选一段文字、鼠标松手在弹窗外 | 弹窗不关闭、选中内容保留（仅「按下+松开都在遮罩上」的点击才关闭弹窗） |

---

**总计: 77 项手工测试 + 4 项自动化测试套件**