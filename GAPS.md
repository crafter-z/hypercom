# HyperCom 基础功能缺口分析

> 生成日期: 2026-07-18
> 依据: `plans/` 目录下 10 份文档 + `AGENTS.md` + 部分源码抽查
> 用途: 指导从 demo 状态到可交付 v0.1 的开发优先级

---

## 总览

HyperCom 架构质量高（Tauri v2 + Rust + React + Zustand，四层分层清晰），已完成大量功能（串口收发、协议解析、语法高亮、分屏树、虚拟滚动、日志落盘等），99 前端 + 27 后端单测全绿。但从文档和测试清单来看，以下能力缺失使其停留在 demo 阶段，无法交付真实用户使用。

---

## P0 — 阻塞交付

### 1. 真实硬件兼容性验证

**现状：** 全部串口功能测试使用 SIM:Loopback（mpsc channel 模拟），从未验证真实 USB-UART 芯片。

**风险：**
- CH340 / CP2102 / FT232 / PL2303 四大主流芯片兼容性未知
- 高波特率（921600 / 1M / 2M bps）实际吞吐未测
- USB 热插拔（连接中拔线）崩溃恢复未验证
- 端口被其他程序占用时的错误表现未验证
- `serialport-rs` 在 Windows 上的已知问题未排查

**建议：** 准备 3 种以上真实 USB-UART 设备，编写硬件测试矩阵文档，逐项通过。

### 2. 错误提示 UX

**现状：** 后端 `CommandError` 枚举（9 变体）设计完整，但前端收到错误后行为不明确。多处使用 `console.debug` 静默处理，用户无法感知故障。

**缺失场景：**
- 串口打开失败（权限不足 / 被占用 / 不存在）→ 用户看到什么？
- 连接中断线（USB 拔出）→ 当前走 `serial:status("disconnected")` 事件，但 UI 是否有明显提示？
- 发送失败（写入错误）→ 静默还是报错？
- 日志写入失败（磁盘满）→ 是否通知用户？
- 数据库初始化失败 → 应用是崩了还是降级运行？

**建议：** 设计统一的错误通知机制（toast / StatusBar 变色 / 弹窗），覆盖所有用户可感知的失败路径。

### 3. 终端内搜索（Ctrl+F）

**现状：** 终端无查找功能。`TerminalView` 使用 `@tanstack/react-virtual`，视口内仅渲染 30-50 行，全量数据在 `useTerminalStore.lines[]` 中。

**要求：**
- Ctrl+F 唤起搜索栏
- 输入关键字 → 滚动到第一个匹配行
- F3 / Enter 跳下一个匹配
- 搜索需要穿透虚拟滚动视口，直接定位到 `lines[]` 中的目标行
- 搜索结果计数（"3/15"）

### 4. 终端选择复制受限

**现状：** `AGENTS.md` 明确注释："全选/复制仅覆盖视口可见行，完整导出走右键菜单"。即用户选中终端文本 Ctrl+C 只能复制屏幕上可见的 30-50 行，其余数据无法选中。

**影响：** 这是串口调试最高频操作之一——看到关键数据 → 选中 → 复制。当前行为与用户预期严重不符。

**建议：** 至少支持 Shift+点击 选择范围跨越多屏行；或提供 "复制全部" / "复制选中行" 右键菜单。

---

## P1 — 严重影响日常使用

### 5. Windows 安装包 & 自动更新

**现状：** `npm run tauri build` 产出裸 .exe。没有安装器、没有代码签名、没有自动更新。

**问题：**
- 无代码签名的 exe 会被 Windows SmartScreen 拦截，普通用户直接劝退
- 无 NSIS/MSI 安装器，用户需手动放置 exe
- 无自动更新意味着每个版本需要用户手动下载覆盖
- Tauri v2 有内置 updater 插件（`tauri-plugin-updater`），但未配置

**建议：** 配置 NSIS 安装器 + `tauri-plugin-updater` + 代码签名证书（或至少提供 ZIP 便携版作为过渡）。

### 6. DTR/RTS/CTS/DSR/RLSD 引脚状态指示灯

**现状：** `ParamsSection` 有 DTR/RTS 勾选框，`set_flow_control` 命令已实现。但没有引脚状态的视觉反馈。

**对比 SSCOM：** SSCOM 在界面上有 DTR、RTS、CTS、DSR、RLSD（CD）、RI 共 6 个指示灯，实时反映串口物理层状态。这是嵌入式工程师判断硬件接线是否正确的第一手段。

**API 就绪度：** `serialport-rs` 支持读取 `write_data_terminal_ready` / `read_data_set_ready` / `read_clear_to_send` / `read_ring_indicator` / `read_carrier_detect` 等方法。后端需新增轮询或事件推送，前端在 StatusBar 增加指示灯组件。

### 7. 数据过滤

**现状：** 终端无差别显示所有 TX+RX 行。

**缺失：**
- 只看 TX / 只看 RX / 查看全部（三态切换）
- 关键字过滤：输入 "error" 只显示包含 "error" 的行
- 暂停显示：数据仍在后台接收，但 UI 冻结，便于仔细查看当前屏幕内容
- 过滤状态下虚拟滚动的行号映射（显示行号 vs 实际 lines[] 索引）

### 8. 终端内时间戳显示增强

**现状：** `formatTimestamp` 硬编码为 `HH:MM:SS.mmm` 格式（`TerminalView.tsx:20`）。配置中有 `timestampMode` 字段，但代码中未发现不同模式的实际实现。

**缺失：**
- 绝对时间：ISO 格式、本地化格式
- 相对时间：距上一条 RX 的时间差（delta-time）
- 距连接开始的运行时间
- 毫秒精度可配置（显示/隐藏）
- HEX 模式下的时间戳显示行为

### 9. 操作面板发送区完善

**现状：** `SendSection.tsx`（150 行）实现了基础 ASCII/HEX 发送 + 行结束符选择 + 发送历史。但缺失以下日常高频需求：

- **发送字节计数：** 输入框旁显示 "当前输入将发送 N 字节"（HEX 模式直接计字节，文本模式根据编码计算）
- **Enter 发送 / Enter 换行 切换：** 嵌入式工程师经常需要输入多行 AT 指令脚本，当前 Enter 无法换行
- **发送历史持久化：** `sendHistoryRef` 仅存内存，关闭应用后历史丢失（限制 50 条）
- **快捷发送按钮：** 可自定义 3-5 个常用命令一键发送（如 `AT\r\n`、`AT+PING\r\n`）
- **定时发送：** 按固定间隔自动重发当前输入（比循环发送更轻量，适合单条命令反复测试）
- **发送后自动追加换行的视觉确认：** 当前勾选换行后，用户在 HEX 模式下看不到会在数据末尾追加什么

### 10. 自动重连

**现状：** 连接中断后用户需手动重连。

**场景：**
- USB 松动 → 串口消失 → 用户插回 → 应自动恢复连接
- 设备重启 → 串口短暂消失后重新枚举 → 应自动重连
- 待机恢复 → 串口可能仍然存在但读写失败 → 应检测并重连

**建议：** 监听 `serial:status("disconnected")` 事件，若原端口仍在列表中（同名端口重新出现），自动调用 `openPort` 恢复连接。重连次数和间隔可配置。

### 11. 键盘快捷键体系

**现状：** 仅实现了 Ctrl+滚轮缩放字体。无快捷键文档。

**至少需要：**
- `Ctrl+Enter` → 发送
- `Ctrl+L` → 清屏
- `Ctrl+F` → 搜索
- `Ctrl+K` → 连接/断开切换
- `Escape` → 关闭弹窗 / 取消搜索

---

## P2 — 改善体验与健壮性

### 12. 会话恢复（Session Restore）

**现状：** 关闭 HyperCom 前打开 3 个 COM 口、2 个分屏——重开后全部消失，需手动逐一重连、重建布局。

**方案：** 在应用退出时序列化 `{ paneTree, 每个 tab 对应的 port + params }` 到配置文件，启动时恢复。已有的 `config.json` 持久化架构可以扩展。注意：恢复时需验证端口是否仍然可用，不可用的静默跳过。

### 13. 日志回放

**现状：** 日志能写、能列文件、能用外部程序打开。但**不能在 HyperCom 内回放**。

**缺失：**
- 选择一个 .log 文件 → 在终端中以原始时间戳逐行回放
- 回放时支持语法高亮和协议着色（等于加载已有数据到 `TerminalLine[]`）
- 回放速度控制（1x / 2x / 5x / 全速）

### 14. 文件发送（Binary Transfer）

**现状：** 只能发送文本或 HEX 字符串。无法发送二进制文件。

**最小可用版本：**
- 选择文件 → 读取全部字节 → 按原始字节发送（不做任何编解码）
- 发送进度条（大文件可能需要分块发送）
- 后续可扩展 XMODEM / YMODEM / ZMODEM 协议支持

### 15. i18n 收尾

**现状：** 218 keys × 2 语言已定义，但仅 7/22 个文件接入了 `t()`。15 个文件保留中文硬编码。

**未接入的文件：**
- `TerminalView.tsx`
- `OperationPanel/` 目录的 `OperationPanel.tsx`、`SendSection.tsx`、`RulesSection.tsx`
- `Sidebar/Sidebar.tsx`
- `ConfigModal/` 目录下 10 个文件

**影响：** 英文用户看到混杂界面（TitleBar 英文 + Sidebar 中文 + ConfigModal 中文），体验比全中文更差。

### 16. 自定义波特率

**现状：** 缺陷 #33（`OperationPanel.tsx:472-474`）声称已修复。但测试清单中无对应验收项。

**验证方式：** 确认 ParamsSection 波特率下拉包含 "其他..." 选项，选择后出现数字输入框可输入任意值（如 1000000），连接生效。

### 17. 协议解析器端到端验证

**现状：** 前端解析引擎（`protocolParser.ts` 363 行，17 cases）和渲染器（`protocolRenderer.ts` 119 行，8 cases）单测全绿。但**完整链路未验证**：真实串口数据 → ProtocolFrameReassembler 状态机重组 → renderProtocolLine 字段着色 → TerminalView 渲染。

测试清单 25 项中完全没有协议解析的验收项。建议用真实设备（如发送已知 Modbus 帧）或构造二进制测试文件走通全链路。

### 18. 连接时长 & 实时速率

**现状：** StatusBar 显示累计 TX/RX 字节数。但缺少：

- 当前端口连接持续时长
- 实时数据速率（bytes/sec，最好有短期滑动平均值）
- 峰值速率记录

这对评估通信质量非常重要——"为什么我的设备这么慢"是嵌入式调试的常见问题。

### 19. 端口名在窗口标题显示

**现状：** `TitleBar` 仅显示应用名和窗口控制按钮。当同时运行多个 HyperCom 窗口或 Alt+Tab 切换时，无法区分哪个窗口对应哪个串口。

**建议：** 活动端口连接后，标题栏显示 `COM3 (115200) — HyperCom`。

---

## P3 — 工程化 & 质量保障

### 20. 测试清单更新 & 真实硬件测试矩阵

**现状问题：** `test-checklist.md` 存在过期内容：
- 第 23 项 "右键终端 → 导出为 TXT → 剪贴板" 已改为文件保存对话框（commit `4f1693e`），但清单未更新
- 全部串口测试依赖 SIM:Loopback，无真实硬件项

**建议：** 
1. 审查并更新全部 25 项测试用例
2. 新增「真实硬件测试矩阵」章节，覆盖芯片型号 × 波特率 × 流控组合

### 21. 长时间运行压测

**现状：** 未进行过以下场景验证：

- 115200bps 连续接收 1 小时 → 检查内存是否持续增长（Zustand store lines[] 受 `maxLines` 限制但需验证）
- 同时连接 4 个真实串口 → 检查 CPU 占用和 UI 响应
- 虚拟滚动 + 高亮引擎 + 协议解析 + 日志写入四者同时工作的性能表现
- 终端行数达到 `maxLines` 上限后 shift 的性能（大量 shift 操作可能影响虚拟滚动）

**建议：** 编写自动化压测脚本（模拟大量串口数据注入），至少跑通 1 小时无内存泄漏、无崩溃。

### 22. 崩溃恢复 & Panic Hook

**现状：** 后端未配置 panic hook。如果 Rust 侧 `unwrap()` 残留触发 panic：

- Tauri 进程直接退出，前端窗口消失
- 用户看不到任何错误信息
- 日志文件可能未 flush（BufWriter 未 `sync_all`）

**建议：** 
1. 配置 `std::panic::set_hook` 在 panic 前 flush 日志并写入 crash report
2. 前端 ErrorBoundary（`App.tsx` 中已实现）捕获 React 崩溃时提供 "重启应用" 按钮
3. 审查代码中的 `unwrap()` 调用（AGENTS.md 记录了 6 处修复但可能有新引入的）

### 23. 测试清单完善

当前测试清单仅 25 项。明显缺失的验收项：

- 分屏嵌套（3 层深度构造，VS Code 树状）
- 标签跨分屏拖拽
- 自定义波特率
- 日志分片续写
- 日志编码切换（GBK/UTF-8/ISO-8859-1）
- 配置取消回滚（defect #07 的回归测试）
- 模拟模式心跳停止后再启用
- 清屏后流量统计不变
- 虚拟滚动 + 语法高亮 + 协议着色同时生效

### 24. 首次启动体验

**现状：** 新用户打开 HyperCom：空列表、无提示、不知道要做什么。

**建议：**
- 检测无串口时，侧边栏显示引导文字："未检测到串口设备，插入 USB 转串口模块后自动刷新，或点击 🧪 启用模拟模式体验"
- 首次启动弹出简短的功能引导（或至少有一个 Help 按钮指向 README）

---

## 补充观察 & 潜在风险

### A. 文档与代码不一致

| 文档描述 | 实际代码 | 影响 |
|---------|---------|------|
| 架构文档说读线程 "50ms 轮询" | `serial/mod.rs:283-308` 实际是阻塞 read + 100ms timeout，无显式 sleep | 性能理解偏差，但实际行为优于文档描述 |
| `test-checklist.md` 第 23 项 "导出到剪贴板" | 实际已改为 `save()` 文件对话框（commit `4f1693e`） | 测试清单过期，按清单验证会失败 |
| 组件树文档未列出 `ViewStrip.tsx` | 代码中存在 `OperationPanel/ViewStrip.tsx`（118 行） | 新接手开发者可能不知道这个组件的存在 |

### B. 架构风险点

- **Pane tree 重构 (2026-07)：** `panes[]` → `paneTree` 是近期大规模改动，递归树结构在边界条件（移除最后标签、拖拽到深层嵌套等）可能潜伏 bug
- **Mutex 竞争：** 真实串口读循环中 `port_clone.lock()` + 发送路径共享同一把锁，高双向流量时发送可能被读循环阻塞
- **串口超时处理：** 100ms timeout + `TimedOut` 分支只是 `continue`，但读线程没有机会检查 `running` 标志——需确认 timeout 后 `running.load()` 能被及时检查

### C. 缺失但优先级较低的能力

| 能力 | 说明 |
|------|------|
| 数据可视化/绘图 | 实时将串口数据绘制为折线图（温度、电压等传感器数据），差异化竞争力但工作量大 |
| 批量发送脚本 | 支持多行脚本一次性发送（非循环发送，而是顺序执行一次） |
| 串口监控（不打开） | 监视 DTR/RTS/CTS 引脚状态变化而不占用端口 |
| 端口预设/收藏 | 保存 "COM3@115200-8N1 + 特定规则集" 为一键加载的预设 |
| 配置导出/导入 | 分享规则集、命令集、协议模板给团队其他人 |
| 多窗口支持 | 可打开多个 HyperCom 窗口，各自独立管理串口 |
| 窗口置顶 | 调试时保持串口工具在 IDE 上方可见 |
| 最小化到系统托盘 | 配合 `closeBehavior` 配置项 |
| 帮助/关于对话框 | 显示版本号、GitHub 链接、快捷键速查 |

---

## 建议执行顺序（供 agent 参考）

```
Phase 1 (打通真实链路):      #1 真机测试 → #2 错误提示 UX → #10 自动重连
Phase 2 (补齐基本功能):      #3 终端搜索 → #4 选择复制 → #8 时间戳增强
Phase 3 (安装分发):          #5 安装包 → #24 首次启动体验
Phase 4 (体验打磨):          #6 引脚指示灯 → #7 数据过滤 → #9 发送区完善 → #11 快捷键
Phase 5 (健壮性):            #12 会话恢复 → #20 测试矩阵 → #21 长时间压测 → #22 崩溃恢复
Phase 6 (收尾):              #15 i18n 收尾 → #17 协议端到端 → #23 测试清单更新
Phase 7 (进阶):              #13 日志回放 → #14 文件发送 → #19 窗口标题
```

每个 Phase 内的项目可并行。
