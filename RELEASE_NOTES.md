# HyperCom v0.3.2

本次更新集中解决 GitHub issue #3 列出的 6 个问题：条件触发功能全面修复（含指定串口）、波特率自定义输入卡顿与无法输入、滚动锁定高频输出脱节、日志格式可配置，以及移除未实现的背景图设置与无意义的漏斗图标。

## 新功能

**条件触发正式可用**
- 修复触发规则完全不生效的问题：接收数据现在会实时匹配所有已启用的触发规则
- 新增「指定串口」选项：每条规则可选择仅对某个串口生效（默认全部串口）
- 「弹出提示」动作：匹配时弹出提示框（含端口与规则名，同规则 1 秒内不重复打扰）
- 「自动回复」动作：匹配时自动向该串口发送配置的回复内容（支持 HEX 格式）
- 移除从未实现的「标记书签」动作（旧配置中残留的书签规则会自动转为「弹出提示」）

**日志格式设置**
- 日志设置新增两个开关：「日志行包含时间戳」与「日志行包含 RX/TX 方向标记」
- 均可独立开关：默认全开（行为与旧版一致），全部关闭时日志仅记录纯数据行

## 改进

**波特率输入**
- 修复选择「其他...」后整个界面卡顿的问题：输入时不再逐键触发界面重渲染与串口参数重配（新增防抖，连续输入只同步一次）
- 修复自定义波特率无法输入的问题：输入到 9600 / 115200 等预设值时输入框不再消失，可完整输入任意波特率（如 96000、1000000）

**滚动锁定**
- 修复大量数据输出时「显示锁定但实际不在底部」的脱节问题：缓冲满后的头部裁剪与测量滞后都会自动重新钉底

## 清理

- 设置界面移除未实现的「背景图片」选项及相关代码
- 串口输出控制栏移除无意义的漏斗图标（「全部/仅TX/仅RX」切换直接显示）

## 下载安装包

- **Windows**：`hypercom_0.3.2_x64-setup.exe`（推荐，NSIS 安装包，支持中英文）或 `hypercom_0.3.2_x64_en-US.msi`
- **macOS (Apple Silicon)**：`hypercom_0.3.2_aarch64.dmg`
- **macOS (Intel)**：`hypercom_0.3.2_x64.dmg`
- **Linux**：`hypercom_0.3.2_amd64.deb` / `hypercom-0.3.2-1.x86_64.rpm` / `hypercom_0.3.2_amd64.AppImage`

> macOS 版本暂未做代码签名与公证，首次打开可能被 Gatekeeper 拦截，请在「系统设置 → 隐私与安全性」中允许运行。详见仓库 `plans/code-signing.md`。

## 自动更新

v0.3.1 及更早版本用户将在应用内收到更新提示，更新弹窗会显示上述更新说明。

完整功能列表与开发文档见仓库 README 与 `AGENTS.md`。
