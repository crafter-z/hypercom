# HyperCom v0.3.3

本次更新集中解决 GitHub issue #4 列出的 10 个问题，并新增**诊断日志查看器**、完善日志体系、修复若干小问题。

## 新功能

**诊断日志查看器**
- 后端 `log::*` 与前端 `console.*` 统一落盘到 `%APPDATA%/hypercom/diag/hypercom-debug.log`（512KB 轮转保留 3 份）
- 「关于 → 诊断日志」弹窗可查看/级别过滤（全部/INFO/WARN/ERROR）/自动刷新/导出/清空
- 配置项「启用诊断日志」可随时开关（默认开启）

**关于界面完善**
- 新增「GitHub 仓库」按钮，一键打开项目主页
- 新增「开源许可证」按钮，弹出技术栈依赖许可证列表（配套文档 `THIRD_PARTY_LICENSES.md`）
- 版本号修正：只显示真实版本（此前错误显示 `v0.1.0 (0.3.2)`）

## 改进

**串口参数实时生效（issue #4-2）**
- 串口已连接时修改波特率等参数**立即生效**，无需断开重连
- 侧边栏 / 标题栏波特率同步更新；重连不再自动回退 115200
- 切换标签时操作面板自动载入该端口已存参数

**高频输出滚动锁定（issue #4-1）**
- 快速大量输出时滚动锁定保持钉在最新一行，不再因虚拟滚动测量滞后逐帧上漂

**备注名与分组持久化（issue #4-9/10）**
- 串口「备注名」与隐藏状态随 config.json 持久化，重启后仍显示
- 修复分组重启丢失：全量保存配置不再覆盖已保存的分组布局

**日志体系完善**
- 后端错误路径补日志（开/关/发/设参数/发文件/重连/存配置失败均有落盘）
- 移除前端噪音日志，对端口枚举失败、非法正则、触发引擎异常等增加限流/去重

## 清理

- 状态栏移除每串口 6 个引脚状态灯（USB-TTL 两针串口无意义，多端口时底栏混乱）
- 移除「为替代 SSCOM / SuperCom 而生」等对比文案
- 移除无用的「帮助」demo 引导（首次运行 Tour）；快捷键帮助保留
- 操作面板日志按钮图标修正（另存为/打开文件/打开目录）

## 其他

- 修复 `window.set_title` 权限缺失（任务栏/Alt-Tab 窗口标题不更新）
- 新增 `.gitattributes` 统一换行符策略（仓库 LF、Windows 检出 CRLF）
- 应用自身诊断日志可直接查看，排障无需开 DevTools

## 下载安装包

- **Windows**：`hypercom_0.3.3_x64-setup.exe`（推荐，NSIS 安装包，支持中英文）或 `hypercom_0.3.3_x64_en-US.msi`
- **macOS (Apple Silicon)**：`hypercom_0.3.3_aarch64.dmg`
- **macOS (Intel)**：`hypercom_0.3.3_x64.dmg`
- **Linux**：`hypercom_0.3.3_amd64.deb` / `hypercom-0.3.3-1.x86_64.rpm` / `hypercom_0.3.3_amd64.AppImage`

> macOS 版本暂未做代码签名与公证，首次打开可能被 Gatekeeper 拦截，请在「系统设置 → 隐私与安全性」中允许运行。详见仓库 `plans/code-signing.md`。

## 自动更新

v0.3.2 及更早版本用户将在应用内收到更新提示，更新弹窗会显示上述更新说明。

完整功能列表与开发文档见仓库 README 与 `AGENTS.md`。
