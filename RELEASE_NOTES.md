# HyperCom v0.2.0

本次更新带来「柔性工作区」——快捷发送面板与终端标签页可弹出为独立的置顶窗口，侧边栏支持完全折叠；并把全部设置重构为单一 `config.json`，配置迁移/备份只需拷贝一个文件。同时修复了高速输出滚动、托盘菜单等一批问题。

## 新功能

**柔性工作区（弹出窗）**
- 快捷发送面板可弹出为独立的加高置顶窗口，命令超出时显示溢出条
- 终端标签页可「分离」为独立弹出窗（标题含端口号），关窗自动回贴标签
- 主窗与弹窗互不干扰，可拖到副屏常驻

**界面**
- 侧边栏支持完全折叠（VSCode 风格），释放横向空间
- 参数区可直接保存 / 删除波特率预设，无需进设置弹窗
- 命令编辑器：新增按钮置于底部，新建命令自动聚焦

**配置与迁移**
- 全部设置（命令集 / 高亮规则 / 协议模板 / 触发规则 / 端口预设 / 工具配置）统一存入单一 `config.json`，拷贝一个文件即可完整迁移到另一台机器
- 配置导出 / 导入修复：可导出到任意位置（桌面 / U 盘），导入恢复全部设置
- 会话快照独立存储，配置备份不再被高频快照冲刷

**日志**
- 日志编码新增 GBK / ISO-8859-1
- 更改日志目录时弹窗确认，可选把旧日志一并迁移到新目录
- 写入更稳健：每 5 秒自动落盘（崩溃最多丢 5 秒）、分片阈值对续写文件精确、另存前强制落盘

## 问题修复

- 高速串口输出时滚动锁定抖动后失效
- 托盘图标右键菜单瞬间消失
- 快捷发送在配置命令集后无反应（首个命令集现自动激活）

## 下载安装包

- **Windows**：`hypercom_0.2.0_x64-setup.exe`（推荐，NSIS 安装包，支持中英文）或 `hypercom_0.2.0_x64_en-US.msi`
- **macOS (Apple Silicon)**：`hypercom_0.2.0_aarch64.dmg`
- **macOS (Intel)**：`hypercom_0.2.0_x64.dmg`
- **Linux**：`hypercom_0.2.0_amd64.deb` / `hypercom-0.2.0-1.x86_64.rpm` / `hypercom_0.2.0_amd64.AppImage`

> macOS 版本暂未做代码签名与公证，首次打开可能被 Gatekeeper 拦截，请在「系统设置 → 隐私与安全性」中允许运行。详见仓库 `plans/code-signing.md`。

## 自动更新

v0.1.0 用户将在应用内收到更新提示。从本版本起，更新弹窗会正确显示上述更新说明。

完整功能列表与开发文档见仓库 README 与 `AGENTS.md`。
