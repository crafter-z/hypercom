# HyperCom v0.3.1

本次更新集中解决 GitHub issue #2 列出的 9 个问题：标签页右键菜单新增批量开关串口与外部工具入口、串口分组自动持久化、端口自然排序修复、操作面板默认高度与参数栏宽度调整、终端搜索字符级高亮（含大输出性能优化），以及 release 安装包移除模拟串口功能。

## 新功能

**标签页右键菜单增强**
- 新增「打开所有标签页 / 断开所有标签页」：一次性连接或断开所有标签页对应的串口，无可连/可断端口时自动禁用
- 新增外部工具三入口（执行 / 终止 / 配置），与串口管理栏右键菜单内容和行为完全一致

**串口分组自动保存**
- 分组的创建、重命名、删除、展开折叠、成员拖拽等所有变更自动保存到配置文件（500ms 防抖），关闭软件不再丢失
- 移除不再需要的「保存布局」按钮

**终端搜索增强**
- 搜索命中行内现在逐字符高亮 query 出现处（当前匹配行加强显示），与用户高亮规则、协议字段着色可叠加
- 性能优化：搜索栏关闭时不再做后台匹配计算；继续输入时只重扫既有匹配与新到达的行，百万级输出下依然流畅

## 改进

**串口管理面板**
- 修复按端口号排序为反直觉的字典序问题：现在 COM1 < COM2 < COM12（数字段按数值比较，兼容各平台端口命名）
- 修复排序 / 拖拽顺序被 3 秒轮询莫名重置的问题：排序改为持久开关（菜单项高亮当前状态），新插入的端口自动落位；排序期间禁用拖拽以免混淆
- 操作面板默认高度从 200px 提高到 280px，首次启动即完整显示发送区与参数区（仍可拖拽调整）
- 参数栏收窄（最宽 300px），把多余空间让给发送区

**构建与分发**
- release 安装包移除模拟串口（SIM:Loopback）按钮及全部相关功能，仅 `npm run tauri dev` 调试模式可用

## 下载安装包

- **Windows**：`hypercom_0.3.1_x64-setup.exe`（推荐，NSIS 安装包，支持中英文）或 `hypercom_0.3.1_x64_en-US.msi`
- **macOS (Apple Silicon)**：`hypercom_0.3.1_aarch64.dmg`
- **macOS (Intel)**：`hypercom_0.3.1_x64.dmg`
- **Linux**：`hypercom_0.3.1_amd64.deb` / `hypercom-0.3.1-1.x86_64.rpm` / `hypercom_0.3.1_amd64.AppImage`

> macOS 版本暂未做代码签名与公证，首次打开可能被 Gatekeeper 拦截，请在「系统设置 → 隐私与安全性」中允许运行。详见仓库 `plans/code-signing.md`。

## 自动更新

v0.3.0 及更早版本用户将在应用内收到更新提示，更新弹窗会显示上述更新说明。

完整功能列表与开发文档见仓库 README 与 `AGENTS.md`。
