# HyperCom v0.3.0

本次更新聚焦终端接收显示：修复串口 RX 输出碎片化与滚动锁定失灵两个长期问题（GitHub issue #1），为高频输出场景重构了整条 RX 接收管线，并在终端滚动条两端新增快捷跳转按钮。

## 改进

**RX 接收管线重构**
- 接收输出改为字节级行聚合：按 CR / LF / CRLF 切行，跨多个事件到达的设备响应不再碎成多行（此前一次输出的首字符经常独占一行）
- 高频接收改为按帧批写：终端更新从「每事件一次」压缩到「每帧最多一次」，高波特率持续数据流下界面依然流畅
- 无换行符结尾的数据在 250ms 静默后自动显示；无换行的二进制流按 4KB 强制切行，缓冲不会无限增长
- 协议解析改为按字节流顺序输出：帧之前出现的非帧字节现在正确显示在帧之前（此前存在流序错乱）
- 编码切换时会先用旧编码冲刷未显示完的尾部数据，避免切换瞬间出现乱码

**终端交互**
- 滚动条两端新增快捷跳转按钮：到顶自动解除滚动锁定，到底重新锁定并跟随最新输出（锁定态按钮点亮）
- 滚动锁定逻辑整体重设计：锁定状态只由您的明确操作改变（图钉按钮 / 跳转按钮 / 手动滚动后停在的位置），不再被新输出、切换标签页等操作莫名解除
- 未锁定时点击锁定按钮，立即跳到最新输出；切回已锁定的标签页保持钉住尾部
- 有数据流入时打开搜索栏不再被新数据顶走；关闭搜索栏后若处于锁定状态会自动回到最新

## 问题修复

- 修复 RX 一次输出的首字符经常单独成行、输出格式完全混乱（issue #1）
- 修复滚动锁定按钮不生效、切换标签页后锁定在下次输出后脱开（issue #1）
- 修复未锁定时点击锁定按钮不会滚动到最新输出（issue #1）
- 修复零延时循环发送时 TX/RX 显示顺序可能错乱（时序保障加固）

## 下载安装包

- **Windows**：`hypercom_0.3.0_x64-setup.exe`（推荐，NSIS 安装包，支持中英文）或 `hypercom_0.3.0_x64_en-US.msi`
- **macOS (Apple Silicon)**：`hypercom_0.3.0_aarch64.dmg`
- **macOS (Intel)**：`hypercom_0.3.0_x64.dmg`
- **Linux**：`hypercom_0.3.0_amd64.deb` / `hypercom-0.3.0-1.x86_64.rpm` / `hypercom_0.3.0_amd64.AppImage`

> macOS 版本暂未做代码签名与公证，首次打开可能被 Gatekeeper 拦截，请在「系统设置 → 隐私与安全性」中允许运行。详见仓库 `plans/code-signing.md`。

## 自动更新

v0.2.x 及更早版本用户将在应用内收到更新提示，更新弹窗会显示上述更新说明。

完整功能列表与开发文档见仓库 README 与 `AGENTS.md`。
