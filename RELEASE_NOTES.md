# HyperCom v0.2.1

本次更新聚焦「操作面板整合」与「后端串口加固」。操作面板把命令集选择、循环开关收进发送区标题行，重复轮数下沉到命令集设置；后端对串口收发与文件发送做了一轮健壮性加固，并为长期零覆盖的串口模块补上单元测试。同时修复了循环发送、收发顺序、浮窗同步等一批问题。

## 改进

**操作面板**
- 命令集选择 + 循环开关 + 编辑按钮收进发送区标题同一行的紧凑头部，删除独立的「循环发送」分区，循环开关运行时呼吸脉动
- 重复轮数移入命令集设置（每个命令集自有「重复」字段，0 = 跟随循环开关），不再占用操作面板空间
- 文件发送支持取消：发送中文件按钮变为「取消发送」，可随时中断；进度条在完成 / 取消 / 出错 / 空文件时都会正确清除
- 快捷发送按钮现显示命令名称（而非命令内容）

**后端串口加固**
- 文件发送可取消；无延时时不再卡死界面其它任务；读文件改异步；任何退出路径都发送完成事件
- 异常断线后重开串口报「access denied」/ 游离线程泄漏 → 现自动清理陈旧句柄；模拟串口断开也会正确通知前端
- 外部工具输出遇非 UTF-8 不再被截断；工具结束后串口重开失败会提示
- 发送字节数与日志记录统一（新增 `build_tx_bytes` 单一来源）
- 新增串口模块单元测试（hex 解析、发送字节构建、参数映射、管理器错误路径等）；此前该模块零测试覆盖

## 问题修复

- 循环发送第二轮起每条命令误用「轮间间隔」、且设了重复轮数会半路停发 → 现按每条命令自身延时发送、按完整轮数精确停止
- 发送时 RX 先于 TX 显示（模拟串口尤甚）→ 现发送行恒先于其回显
- 配置命令集未点保存时，独立快捷发送浮窗看不到新命令 → 现编辑实时同步到浮窗
- 操作面板拖矮时发送区按键叠到其它控件上 → 现改为分区滚动，不再叠压
- 高亮规则下拉实为无效控件，已移除（高亮在设置页按规则集启用）

## 下载安装包

- **Windows**：`hypercom_0.2.1_x64-setup.exe`（推荐，NSIS 安装包，支持中英文）或 `hypercom_0.2.1_x64_en-US.msi`
- **macOS (Apple Silicon)**：`hypercom_0.2.1_aarch64.dmg`
- **macOS (Intel)**：`hypercom_0.2.1_x64.dmg`
- **Linux**：`hypercom_0.2.1_amd64.deb` / `hypercom-0.2.1-1.x86_64.rpm` / `hypercom_0.2.1_amd64.AppImage`

> macOS 版本暂未做代码签名与公证，首次打开可能被 Gatekeeper 拦截，请在「系统设置 → 隐私与安全性」中允许运行。详见仓库 `plans/code-signing.md`。

## 自动更新

v0.2.0 及更早版本用户将在应用内收到更新提示，更新弹窗会显示上述更新说明。

完整功能列表与开发文档见仓库 README 与 `AGENTS.md`。
